{
  "name": "ai-image-factory-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "workers/*"
  ],
  "scripts": {
    "typecheck": "tsc -p packages/domain --noEmit",
    "test": "node --experimental-vm-modules node_modules/vitest/dist/cli.js run",
    "test:watch": "vitest",
    "lint": "echo 'lint placeholder'"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.1.0",
    "@types/node": "^22.13.10"
  }
}
